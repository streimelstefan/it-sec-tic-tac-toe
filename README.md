# It Security Server Side Request Forgery

Zum starten der Applikation wird Docker benötigt.

Wenn docker installiert ist kann die Applikation einfach über:

´´´
docker compose up -d
´´´

gestartet werden.
